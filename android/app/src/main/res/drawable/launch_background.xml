<?xml version="1.0" encoding="utf-8"?>
<!-- Native Android launch background — shown before Flutter engine starts.
     Keep this as a solid brand-color only. The Luvco logo animation
     is handled entirely by SplashScreen (Flutter widget).              -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_color" />
</layer-list>
